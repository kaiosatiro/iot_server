name: Continuous Integration

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

concurrency:
  group: master
  cancel-in-progress: true

jobs:
  quality-assurance:
    name: Quality Assurance
    uses: ./.github/workflows/qa.yml

  deploy:
    name: Deploy
    needs: quality-assurance
    
    runs-on: ubuntu-latest
    steps:
      - name: Configure SSH
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
        run: |
          mkdir -p ~/.ssh
          echo "$SSH_PRIVATE_KEY" > ~/.ssh/github
          chmod 600 ~/.ssh/github
          cat >> ~/.ssh/config << END
          Host Target
            HostName $SSH_HOST
            User $SSH_USER
            IdentityFile ~/.ssh/github
            LogLevel ERROR
            StrictHostKeyChecking no
          END
      - name: Create env file
        id: create-env
        env:
          LOG_INFO_LOCAL_PATH: ${{ env.LOG_INFO_LOCAL_PATH }}
          LOG_INFO_LOCAL_PATHV: ${{ vars.LOG_INFO_LOCAL_PATH }}
          
        run: |
          echo ${{ env.LOG_INFO_LOCAL_PATH }}
          echo LOG_INFO_LOCAL_PATH=$LOG_INFO_LOCAL_PATH > .env

          echo ${{ vars.LOG_INFO_LOCAL_PATH }}
          echo LOG_INFO_LOCAL_PATH=$LOG_INFO_LOCAL_PATHV >> .env

      - name: Pull repository
        run: |
          ssh Target "cd iot_server && git pull origin main"
      - name: Send env file
        run: |
          scp .env Target:iot_server/.env
      - name: Deploy
        if: steps.create-env.outcome == 'success'
        run: |
            ssh Target "cd iot_server && sudo docker compose -f docker-compose.yml up -d --build"

